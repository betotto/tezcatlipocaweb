import React, {PropTypes} from 'react';

const Footer = () => {
	return (
		<footer>
			<div className="content">
				{'This app should have differences'}
			</div>
		</footer>
	);
};

Footer.propTypes = {
};

export default Footer;
